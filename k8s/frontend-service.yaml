apiVersion: v1
kind: Service
metadata:
  name: bookflow-frontend
  namespace: bookflow
spec:
  type: NodePort
  selector:
    app: bookflow-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
